<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title - Flash Cards</title>
    <link rel="stylesheet" href="~/Content/styles.css">
    <link rel="stylesheet" href="~/Content/bootstrap.css">


    <script src="~/Scripts/jquery-3.1.1.min.js"></script>
    <script src="~/Scripts/jquery.validate.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
</head>
<body>
    <div class="container">
        <div>
            <div class="nav navbar">
                <div>@Html.ActionLink("Home", "Index", "Home", new {@class="list_item btn" })</div>
                <div>@Html.ActionLink("Decks", "Index", "Deck", new { @class = "list_item btn" })</div>
                <div>@Html.ActionLink("Cards", "Index", "Card", new { @class = "list_item btn" })</div>
                <div>@Html.ActionLink("Register", "Register", "Home", new { @class = "list_item btn" })</div>
                <div>@Html.ActionLink("Login", "Login", "Home", new { @class = "list_item btn" })</div>
                <div>@Html.ActionLink("Logout", "Logout", "Home", new { @class = "list_item btn" })</div>
            </div>
        </div>
    </div>
    <div>
        @RenderBody()
    </div>
</body>
</html>
